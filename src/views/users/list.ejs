<!DOCTYPE html>
<html lang="es">

<%- include("../partials/head.ejs")%>

<body>

    <%- include("../partials/header.ejs") %>

    <main>
        <% if (userList == "") { %>
            <h1>
                <p>No hay nadie ingresado. </p> 
                <a href="/users/register">Sé el primero!</a>
            </h1>
        <% } else{ %>  
                <ul>
                    <% for (let i = 0; i< userList.length; i++) {%>
                        <li>
                            <%= userList[i].name %>
                            <%= userList[i].email %>
                            <%= userList[i].password %>
                        </li>
                        <form action="/users/delete/<%=userList[i].name %>?_method=delete" method="POST">
                            <button type="submit">
                                Eliminar
                            </button>
                        </form>
                        <form action="/users/update/<%=userList[i].email %>?_method=put" method="POST">
                            <button type="submit">
                                Modificar
                            </button>
                        </form>
                    <% } %>
                </ul>
            <% } %>
            <a href="/users/register">volver</a>

    </main>

    <%- include("../partials/footer.ejs") %>
    
</body>

</html>